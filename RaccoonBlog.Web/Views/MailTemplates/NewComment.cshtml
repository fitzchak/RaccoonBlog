@model NewCommentEmailViewModel
<h1>
	A new @(Model.IsSpam ? "spam" : null)
	comment on @Model.BlogName blog</h1>
<strong>Post:</strong> <a href="@Url.RelativeToAbsolute(Url.Action("Details", "PostDetails", new { id = Model.PostId, slug = Model.PostSlug, key = Model.Key }))">@Model.PostTitle</a>
<ul>
	<li><strong>Author:</strong> @Model.Author</li>
	<li><strong>Url:</strong> @Model.Url</li>
	<li><strong>Email:</strong> @Model.Email</li>
	<li><strong>CreatedAt:</strong> @Model.CreatedAt</li>
</ul>
<strong>Body:</strong>
<p>@Model.Body</p>
<p>
	<a href="@Url.RelativeToAbsolute(Url.Action("Details", "PostDetails", new { id = Model.PostId, slug = Model.PostSlug, key = Model.Key }))#comment@(Model.Id)">
		View Comment</a>
</p>
<p>
	<a href="@Url.RelativeToAbsolute(Url.Action("Details", "Posts", new { id = Model.PostId, slug = Model.PostSlug, area = "Admin" }))#comment@(Model.Id)">
		View Comment In Admin Page</a>
</p>
@if (Model.IsSpam)
{
	<p>
	  <a href="@Url.RelativeToAbsolute(Url.Action("DeleteAllSpamComments", "Posts", new { area = "Admin" }))#comment@(Model.Id)">Delete All Spam Comments</a>
	</p>
}